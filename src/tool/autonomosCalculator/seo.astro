---
import { SEORenderer } from '@jjlmoya/utils-shared';
import type { ToolLocaleContent } from '../../types';
import type { AutonomosCalculatorUI } from './ui';

interface Props {
  content: ToolLocaleContent<AutonomosCalculatorUI>;
  locale?: string;
}

const { content, locale = 'es' } = Astro.props;
---

<SEORenderer content={{ locale, sections: content.seo }} />
